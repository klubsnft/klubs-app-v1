import { View, ViewParams } from "skyrouter";
export default class AddMetaverse implements View {
    private container;
    private input;
    constructor();
    private loadAddress;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=AddMetaverse.d.ts.map