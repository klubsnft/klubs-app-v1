import { View, ViewParams } from "skyrouter";
export default class PFPs implements View {
    private container;
    constructor();
    private load;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=PFPs.d.ts.map