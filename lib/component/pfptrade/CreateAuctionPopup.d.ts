import { DomNode, Popup } from "@hanul/skynode";
export default class CreateAuctionPopup extends Popup {
    private addr;
    private id;
    content: DomNode;
    private loading;
    private info;
    private startPriceInput;
    private endBlockInput;
    constructor(addr: string, id: number);
    private load;
}
//# sourceMappingURL=CreateAuctionPopup.d.ts.map