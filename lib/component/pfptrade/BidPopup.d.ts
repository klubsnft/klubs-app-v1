import { DomNode, Popup } from "@hanul/skynode";
export default class BidPopup extends Popup {
    private addr;
    private id;
    content: DomNode;
    private loading;
    private list;
    private inputs;
    constructor(addr: string, id: number);
    private load;
}
//# sourceMappingURL=BidPopup.d.ts.map