import { DomNode } from "@hanul/skynode";
export default class ItemSaleCard extends DomNode {
    private metaverseId;
    private addr;
    private saleIndex;
    constructor(metaverseId: number, addr: string, saleIndex: number);
    private load;
}
//# sourceMappingURL=ItemSaleCard.d.ts.map