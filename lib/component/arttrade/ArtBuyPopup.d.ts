import { DomNode, Popup } from "@hanul/skynode";
export default class BuyPopup extends Popup {
    private ids;
    content: DomNode;
    private loading;
    private list;
    private prices;
    private mileages;
    constructor(ids: number[]);
    private load;
}
//# sourceMappingURL=ArtBuyPopup.d.ts.map