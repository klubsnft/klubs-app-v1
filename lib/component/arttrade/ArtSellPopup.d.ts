import { DomNode, Popup } from "@hanul/skynode";
export default class SellPopup extends Popup {
    private ids;
    content: DomNode;
    private loading;
    private list;
    private inputs;
    constructor(ids: number[]);
    private load;
}
//# sourceMappingURL=ArtSellPopup.d.ts.map