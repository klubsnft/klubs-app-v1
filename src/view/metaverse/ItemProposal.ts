import { View, ViewParams } from "skyrouter";

export default class ItemProposal implements View {

    constructor() {
    }

    public changeParams(params: ViewParams, uri: string): void { }

    public close(): void {
    }
}
